# ── Backend API ────────────────────────────────────────
# Used at build time by Vite. When running in Docker, override at runtime:
#   docker run -e VITE_API_URL=https://api.example.com ...
VITE_API_URL=http://localhost:3000/api/v1
